version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weathergo-frontend
    ports:
      - "8080:80"
    environment:

      - BACKEND_URL=http://host.docker.internal:5002
    restart: unless-stopped

